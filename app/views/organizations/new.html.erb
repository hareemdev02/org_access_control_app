<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card">
      <div class="card-header">
        <h4>Create New Organization</h4>
      </div>
      <div class="card-body">
        <%= form_with(model: @organization, local: true) do |form| %>
          <% if @organization.errors.any? %>
            <div class="alert alert-danger">
              <h6><%= pluralize(@organization.errors.count, "error") %> prohibited this organization from being saved:</h6>
              <ul>
                <% @organization.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="form-group">
            <%= form.label :name, class: 'form-label' %>
            <%= form.text_field :name, class: 'form-control', placeholder: 'Enter organization name' %>
          </div>

          <div class="form-group">
            <%= form.label :domain, class: 'form-label' %>
            <%= form.text_field :domain, class: 'form-control', placeholder: 'example.com' %>
          </div>

          <div class="form-group">
            <%= form.label :description, class: 'form-label' %>
            <%= form.text_area :description, class: 'form-control', rows: 3, placeholder: 'Describe your organization' %>
          </div>

          <div class="form-group">
            <%= form.label :participation_type, class: 'form-label' %>
            <%= form.select :participation_type, 
                options_for_select([
                  ['Public - Open to everyone', 'public'],
                  ['Restricted - Age-based restrictions', 'restricted'],
                  ['Private - Organization members only', 'private']
                ], @organization.participation_type), 
                {}, 
                class: 'form-control' %>
          </div>

          <div class="form-actions">
            <%= form.submit 'Create Organization', class: 'btn btn-primary' %>
            <%= link_to 'Cancel', organizations_path, class: 'btn btn-secondary' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div> 